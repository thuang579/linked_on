<%= form_for @skill do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>


<%=f.label :user %>
<%= f.select :user_id, options_from_collection_for_select(User.all, :id, :name) %>


<%= f.submit 'Create a skill'%>
<% end %>